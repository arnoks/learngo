package main

/*
import (
	"bytes"
	"io"
	"os"
	"testing"
	"time"
)

// TestStuff check out some interface aspects
func TestStuff(t testing.T) {
	var w io.Writer
	w = os.Stdout
	w = new(bytes.Buffer)
	w = time.Second // does not compile

	var rwc io.ReadWriteCloser
	rwc = os.Stdout
	rwc := new(bytes.Buffer) // does not compile

	w = rwc
	rwc = w // does not compile

}
*/
